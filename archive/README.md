# Historical prototypes

These files are preserved for reference and are not Cargo targets or installed resources.
Current code lives under `src/`; the supported overlay uses `src/overlay/`.

- `legacy-overlay/`: original windowed and layer-shell source snapshots.
- `conky/clock.conf`: the early Conky clock prototype.

Make production changes in the active modules rather than updating these snapshots.
